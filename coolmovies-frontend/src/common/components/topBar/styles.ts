import { css } from "@emotion/react";

const styles = {
  root: css({
    height: "40px",
    width: "100vw",
    padding: "0px 20px",
    display: "flex",
    justifyContent: "flex-start",
    alignItems: "center",
    flexDirection: "row",
  }),
  title: css({
    marginLeft: "10px",
  }),
};

export default styles;
