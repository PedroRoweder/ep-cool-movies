import { css } from "@emotion/react";

const styles = {
  review: css({
    padding: "15px",
    borderRadius: "5px",
    backgroundColor: "#eee",
    marginBottom: "32px",
  }),
};

export default styles;
